import * as icons from '../icons/receipts.js';

export const RECEIPT_TYPES = {
    FOOD: 0,
};

export const RECEIPT_LABELS = {
    [RECEIPT_TYPES.FOOD]: 'Food',
};

export const RECEIPT_ICONS = {
    [RECEIPT_TYPES.FOOD]: icons.food,
};